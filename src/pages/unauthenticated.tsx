import React, { FC } from 'react';
import { Typography } from '@mui/material';

const UnauthenticatedPage: FC = function UnauthenticatedPage() {
  return (
    <Typography>
      TODO: Include instructions on how to authenticate using the slash command on Discord.
    </Typography>
  );
};

export default UnauthenticatedPage;
